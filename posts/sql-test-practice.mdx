---
title: "SQL 기초: 데이터베이스 쿼리 작성의 핵심 개념"
date: "2024-06-05"
description: "데이터베이스 작업의 기본이 되는 SQL 기초 문법과 사용 방법에 대해 알아봅니다."
author: "김데이터"
tags: ["Next.js", "MDX", "Sample"]
image: "/sql-test-practice/main.png"
---

# SQL이란 무엇인가?

**SQL(Structured Query Language)**은 데이터베이스 관리 시스템(DBMS)에서 데이터를 관리하고 조작하기 위한 표준 언어입니다. SQL을 사용하면 데이터베이스에서 데이터를 **저장, 검색, 수정, 삭제**할 수 있으며, 데이터베이스 구조를 정의하고 관리할 수 있습니다.

## SQL의 주요 구성 요소

SQL은 크게 다음과 같은 구성 요소로 나눌 수 있습니다:

1. **DDL (Data Definition Language)**: 데이터베이스 구조를 정의하는 명령어
2. **DML (Data Manipulation Language)**: 데이터를 조작하는 명령어
3. **DCL (Data Control Language)**: 데이터 접근 권한을 제어하는 명령어
4. **TCL (Transaction Control Language)**: 트랜잭션을 제어하는 명령어

## SELECT 문 기초

가장 많이 사용되는 SQL 명령 중 하나는 **SELECT** 문입니다. 이 명령을 사용하여 데이터베이스에서 데이터를 검색할 수 있습니다.

기본 SELECT 문의 형식은 다음과 같습니다:

```sql
SELECT column1, column2, ...
FROM table_name
WHERE condition;
```

예를 들어, 'employees' 테이블에서 모든 직원의 이름과 급여를 검색하려면:

```sql
SELECT first_name, last_name, salary
FROM employees;
```

특정 조건을 충족하는 데이터만 검색하려면 WHERE 절을 사용합니다:

```sql
SELECT first_name, last_name, salary
FROM employees
WHERE salary > 50000;
```

## JOIN을 활용한 테이블 연결

**JOIN**은 여러 테이블의 데이터를 결합하는 강력한 기능입니다. 관계형 데이터베이스의 핵심 기능 중 하나로, 데이터의 관계를 표현할 수 있습니다.

가장 일반적인 JOIN 유형은 다음과 같습니다:

1. **INNER JOIN**: 두 테이블에서 일치하는 행만 반환
2. **LEFT JOIN**: 왼쪽 테이블의 모든 행과 오른쪽 테이블에서 일치하는 행 반환
3. **RIGHT JOIN**: 오른쪽 테이블의 모든 행과 왼쪽 테이블에서 일치하는 행 반환
4. **FULL JOIN**: 두 테이블의 모든 행 반환

다음은 INNER JOIN의 예시입니다:

```sql
SELECT e.first_name, e.last_name, d.department_name
FROM employees e
INNER JOIN departments d ON e.department_id = d.department_id;
```

## 집계 함수와 GROUP BY

**집계 함수**는 데이터 집합에 대한 계산을 수행합니다. 주요 집계 함수로는 COUNT, SUM, AVG, MAX, MIN 등이 있습니다.

**GROUP BY** 절과 함께 사용하면 데이터를 그룹화하고 각 그룹에 대한 집계를 수행할 수 있습니다.

```sql
SELECT department_id, AVG(salary) as avg_salary
FROM employees
GROUP BY department_id;
```

위 쿼리는 부서별 평균 급여를 계산합니다.

그룹화된 결과에 조건을 적용하려면 **HAVING** 절을 사용합니다:

```sql
SELECT department_id, AVG(salary) as avg_salary
FROM employees
GROUP BY department_id
HAVING AVG(salary) > 60000;
```

## 서브쿼리 활용하기

**서브쿼리(Subquery)**는 쿼리 내에 중첩된 또 다른 쿼리입니다. 복잡한 조건이나 계산을 수행할 때 유용합니다.

```sql
SELECT first_name, last_name, salary
FROM employees
WHERE salary > (SELECT AVG(salary) FROM employees);
```

위 쿼리는 평균 급여보다 높은 급여를 받는 직원들을 찾습니다.

## 정리

SQL은 데이터베이스 작업을 위한 필수적인 언어입니다. 기본 문법을 익히고 나면, **데이터 분석, 웹 애플리케이션 개발, 비즈니스 인텔리전스** 등 다양한 분야에서 활용할 수 있습니다.

더 복잡한 SQL 기능으로는 **윈도우 함수, CTE(Common Table Expressions), 재귀 쿼리** 등이 있으며, 이러한 고급 기능을 익히면 더 효율적인 데이터베이스 쿼리를 작성할 수 있습니다.
